<%- include('partials/head') %>

</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">
<%- include('partials/nav') %>

  <main class="py-4 px-4">
  <div class="flex flex-col items-center justify-center min-h-screen px-4">
    <div class="text-center">
      <h1 class="text-blue-600 text-3xl font-bold mb-4">Welcome to Our Airbnb</h1>
      <p class="text-md text-gray-600 mb-12">Discover and manage beautiful homes in one place</p>
      
      
        <ul class="mt-8 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 w-full max-w-4xl">
          <% homes.forEach(home => { %>
            <li class="bg-white rounded-lg shadow-md p-4 hover:shadow-lg transition-shadow border-l-4 border-blue-600">
          <p class="text-gray-800 font-semibold"><%= home.houseName %></p>
            </li>
          <% }); %>
        </ul>
        </ul>
      </div>
    </div>
  </div>
  </main>
</body>
</html>